name: web-searxng
version: 1.0.0
description: "SearXNG web search — free, self-hosted, privacy-respecting metasearch engine. Supports URLs with embedded Basic Auth credentials (http://user:password@host:port). Requires SEARXNG_URL pointing at your instance."
author: NousResearch
kind: backend
provides_web_providers:
  - searxng
